import { store as auth } from '../modules/auth'
import { store as components } from '../components'
import { store as single } from '../pages/single'

export default {
  auth,
  components,
  single
}
