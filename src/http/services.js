import { services as auth } from '@/modules/auth'
import { services as components } from '@/components'
import { services as single } from '@/pages/single'
import { services as streams } from '@/pages/streams'

export default {
  auth,
  components,
  single,
  streams
}
